[package]
name = "gpui-react-macros"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Derive macros for gpui-react props schemas"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["derive", "parsing", "printing"] }