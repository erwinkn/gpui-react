---
"@gpuix/native": patch
"@gpuix/react": patch
---

Add tested fork runtime archives with matching native dependencies, source refs,
and SHA-256 hashes. The default native archive supports macOS arm64 and includes
the browser WASM runtime. Consumer compositions use the explicit runtime loader.
