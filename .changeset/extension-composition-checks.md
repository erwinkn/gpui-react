---
"@gpuix/native": patch
"@gpuix/react": patch
---

Add a runnable framework-only extension composition example. Verify the public
loader and texture API with native GPU pixels, source and relocated compiled
workers, and browser WebGPU/WebGL pixel tests. Keep the example and its test
dependencies in a private fixture package.
