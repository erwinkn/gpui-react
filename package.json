{
  "name": "gpui-react",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "fixtures/bridge-counter",
    "fixtures/bridge-gpu-component",
    "fixtures/bridge-pierre",
    "scripts"
  ],
  "scripts": {
    "build": "bun run --cwd packages/bridge build && bun run --cwd packages/bridge-controls build",
    "test": "bun run --cwd packages/bridge test"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-reconciler": "^0.33.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/react": "^19.2.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
