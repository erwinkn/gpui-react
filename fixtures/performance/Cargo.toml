[package]
name = "gpui-react-frame-cost"
version = "0.1.0"
edition = "2024"
publish = false

[features]
allocation-counts = []
scene-checks = ["gpui/test-support", "gpui_macos/test-support"]

[dependencies]
gpui = { workspace = true }
gpui_macos = { workspace = true }
gpui-react = { path = "../../crates/gpui-react" }
gpui-react-kit = { path = "../../crates/gpui-react-kit" }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }